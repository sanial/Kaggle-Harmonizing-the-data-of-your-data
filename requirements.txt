# CPU-only default. For GPU training install the CUDA wheel instead:
#   pip install "torch==2.6.0+cu124" --index-url https://download.pytorch.org/whl/cu124 --force-reinstall --no-deps
torch>=2.0.0
transformers>=4.30.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
pyyaml>=6.0
tqdm>=4.65.0
